<% require 'csv' %>
<%= CSV.generate_line @label_presenter.headers %>
<%- @label_presenter.each_label do |label| -%>
  <%=raw CSV.generate_line [label.order_code, label.title, label.first_name,
                            label.last_name, label.address] -%>
<%- end -%>
