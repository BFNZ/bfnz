<% if coordinator %>
  <td><%= coordinator.name %></td>
  <td><%= coordinator.email %></td>
  <td>
    <div class='btn-group-vertical'>
      <%= link_to 'Edit', edit_admin_coordinator_path(coordinator), class: 'btn btn-default' %>
    </div>
  </td>
<% else %>
  <td colspan="3"></td>
<% end %>
