<%= CSV.generate_line ["Coordinator",
                       @presenter.coordinator_name,
                       @presenter.coordinator_email] %>

<%- headers = ["Customer ID",
               "Title",
               "First name",
               "Last name",
               "Address",
              ]-%>

<%= CSV.generate_line headers %>
<%- @presenter.contacts.each do |contact| -%>
  <%=raw CSV.generate_line([contact.id,
                            contact.title,
                            contact.first_name,
                            contact.last_name,
                            contact.address,
                           ]) -%>
<%- end -%>
